#cloud-config
# Proxmox-specific user data. The creation script replaces the SSH key marker.
users:
  - name: admin
    gecos: Admin
    groups: [adm, sudo]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: true
    ssh_authorized_keys:
      - |-
          @@SSH_PUBLIC_KEY@@
